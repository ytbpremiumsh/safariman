// Temporary cost-saving lock requested by the project owner.
// Keep enabled until an explicit decision to resume AI and deploy again.
export const AI_PAUSED = true;
export const AI_PAUSED_MESSAGE = "Fitur AI dinonaktifkan sementara untuk menghemat kredit.";
